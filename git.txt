git (버전관리 프로그램)

버전을 만들어서 관리하는 과정
작업공간(작업) -> 스테이지(임시저장) -> 커밋(버전확정)

명령어로 관리하는 방법
1. 변경된 파일을 스테이지로 올리는법 : gut add.
2. 버전을 확정하는 방법 : git commit -m "메시지"

프로그램으로 관리하는 방법
ㄴ githubdesktop

1. 작업공간에서 작업을 하면 자동으로 스테이지에 올라간다.
2. 메시지 적고
3. push origin 버튼을 눌러야 github로 올라감