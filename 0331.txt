싱글톤 패턴
객체를 메모리에 한 번만 올리고, 올라간 객체를 계속 사용하는 방식
1. 생성자를 private으로 정의해서 외부에서 호출하지 못하도록 한다.
2. 클래스 내부에서 static으로 객체를 하나 생성한다.(private)
3. 클래스 내부에서 만든 객체를 사용할 수 있도록 public으로 된 메서드를 만든다.

상속
부모 클래스의 필드나 메서드를 자식 클래스에서 사용할 수 있는 것
자식 클래스가 상속받을 클래스를 고른다.

클래스명 extends 상속받을 클래스명{

}

오버라이딩(메서드의 재정의)

Animal클래스를 만든다.
-sound()메서드를 만든다.
-Dog,Cat클래스는 Animal클래스를 상속받는다.
-Dog,Cat에서 sound()를 오버라이딩 한다.
-main에서 각 객체의 sound()를 호출했을 때 다음과 같은 결과를 출력하세요
동물이 소리를 냅니다.
강아지가 멍멍 짖습니다.
고양이가 야옹 웁니다.

Employee클래스
필드
-name
메서드
- getpay() : 0을 반환
RegularEmployee 클래스
필드
- salary
getPay()를 오버라이딩 하여 월급을 반환한다.
PrartTimeEmployee클래스
필드
- 시급
- 근무시간
getPay() 오버라이딩 하여 급여를 반환
Main에서 각 직원의 이름과 급여 출력하기
정규직 김철수의 급여 : 3000000
아르바이트 이영희의 급여 : 5000000

각 클래스의 필드는 생성자를 통해 초기화 해주세요

각 개념의 숙지
클래스에 어떤게 포함될수 있는지
필드 
메서드
생성자

클래스 타입변환
-상속관계에서만 타입변환이 가능
자동타입변환 : 자식 -> 부모타입
- 자식클래스에 정의한 멤버를 사용할 수 없다.
강제타입변환 : 부모타입 -> 자식타입
-자식클래스에 정의한 멤버를 사용할 수 있다.

Payment클래스
메서드
pay(int amount); : xx원을 결제합니다.

CardPayment, KakaoPayment클래스는 Payment를 상속받는다.
각 클래스에서 pay메서드를 오버라이딩 한다.
"카드로 xx원을 결제합니다."
"카카오페이로 xx원을 결제합니다."

Order클래스
-Payment타입의 필드를 가진다

main에서 결제 방식을 변경하면서 서로 다른 결과가 나오도록 작성하세요

Animal 클래스
메서드
sound() : "동물이 운다"

Dog, Cat 클래스는 Animal클래스를 상속받는다.
sound()오버라이딩 해서 "멍멍","야옹"

zoo클래스를 만들고
printSound()메서드를 정의
매개변수로 Animal animal을 가진다.
메서드 내부에서 animal.sound()를 실행

Main에서 zoo객체를 만들고 Dog,Cat객체를 전달하여 결과를 확인하기