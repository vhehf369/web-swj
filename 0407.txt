Class 클래스
클래스에 대한 정보를 얻고 싶을때 사용한다.
필드,메서드,생성자에 대한 정보도 얻을 수 있다.

어노테이션을 직접 만드는일은 거의 없다

하지만 자바에서 제공하거나 라이브러리가 제공하는 어노테이션을 사용하는 일이 많다.

배열 개념
-선언
-생성(초기화 리스트, 빈 배열)

생성자 개념
-구조

오버로딩

메서드
-구조
-매개변수
-반환타입, 반환값

중첩타입 파라미터
-제네릭 안에 제네릭 타입이 들어가는 구조
Outer<Inner<T>>
복잡한 데이터 구조 표현 가능
타입 안정성 유지
객체 관계를 명확하게 표현

제네릭 메서드
메서드 자체가 타입을 일반화 해서 받는 메서드
클래스 전체를 제네릭으로 만드는게 아니라 특정 메서드 하나만 필요할때
타입을 유연하게 처리하는 방법

접근제한자 <T> 반환타입 메서드명(매개변수){

}

//배열을 외부에서 전달받아 화면에 출력하는 기능을 가진
	//printArray 제네릭 메서드 작성하기
	
	//Main에서 다음의 배열들을 출력하기
	//String [] name = {"김철수", "이영희", "박민수"};
	//Integer [] nums = {10,20,30};

StudentPrinter클래스를 만든다.
제네릭 메서드 printStudent(T name, U age를 작성한다)
이름은 문자열, 나이는 정수로 테스트하시오